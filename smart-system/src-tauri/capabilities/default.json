{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Grundrechte des Smart Systems: Core-APIs fuer beide Fenster plus Autostart-Verwaltung aus der UI. Bewusst schmal — Dateizugriff, Shell und alles Weitere kommen erst mit den Sandbox-Werkzeugen (Phase 5) als eigene, eng begrenzte Capabilities.",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "dialog:allow-open",
    "notification:default"
  ]
}
