apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: msm
resources:
  - 00-namespace.yaml
  - 20-postgres.yaml
  - 30-config.yaml
  - 40-panel.yaml
  - 50-ingress.yaml
  - 60-networkpolicy.yaml
# 10-secrets.example.yaml ist bewusst NICHT enthalten: Secrets werden ausserhalb
# des Repositories angelegt (siehe README).
