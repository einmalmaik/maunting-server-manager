{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the MSM Backup Recovery app. Grants core, dialog, and scoped fs permissions so the M1 UI can pick and read .enc backup files for offline DIS decryption.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
