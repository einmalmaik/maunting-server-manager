{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the MSM Backup Recovery app. Tauri file-dialog and fs permissions are granted in the `recovery-basic-ui` / `rust-extraction` features.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
